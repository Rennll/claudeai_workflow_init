# protocol.md — 工作協議

## Session 開場流程
每個 session 開始時依序讀取：
1. `project.md`（專案方向與原則）
2. `handoff.md`（當前狀態與下一步）

確認本 session 的單一交付目標後才開始執行。

## 偏移偵測
遇到以下三類情況立即暫停，說明觀察到的偏移，問使用者要修正方向、調整藍圖、還是這本來就是計畫的一部分：
- **目標偏移**：當前子任務不再直接服務主線目標
- **成本偏移**：某個工具或格式問題佔據比主線任務更多的討論篇幅
- **證明偏移**：開始追求「讓某技術細節能運作」而非完成功能

## 任務確認
開始執行前先用一句話說明：
- 理解的目標是什麼
- 完成的條件是什麼

確認後才開始執行。

## Wrap-up 規則
- 任務到達自然收束點時主動建議 wrap up，使用者確認後才產出輸出
- 每個 session 結束必須產出完整 `handoff.md` 與 commit 指令
- `handoff.md` 的 History 只保留「影響現狀但原因不明顯的決策」與「容易被重複提議但行不通的方向」，其餘刪除。沒有時間順序的要求
- 藍圖方向改變時同步更新 `project.md` 並記錄原因與版本
- Wrap-up 時主動掃描本次對話，產出「建議更新設定」草稿，等使用者確認後才寫入正式檔案

遇到以下情況，在回答前先說「⚠️ 狀態確認：xxx」再繼續：
- 同一問題修超過兩輪未解決——停下來說明可能的根本原因，確認方向後再繼續
  
以下情況視脈絡判斷是否需要確認，不強制打斷：
- 回報 bug 但未說明是否使用最新版本
- 同時提出兩個以上不同性質的問題（使用者以 btw 標記者略過）

## 互動偏好
- 輸出程式碼時預設用程式碼區塊。腳本或可執行程式需要在容器內執行時，建立為檔案並說明用途
- commit message 由 AI 根據本次 session log 產出，格式為 git commit -m "..."