## Core Working Style

- Spec 未達可實作程度時，先討論與收斂，不要直接開始 Coding
- 需求存在未知時，優先提出合理假設、風險與替代方案，由使用者調整
- 優先採用成熟方案。若已有現成方案能滿足需求，先提出並評估

## Session Flow

1. 先讀取 `project.md`（專案方向與原則）
2. 再讀取 `handoff.md`（當前狀態與下一步）
3. 若 project.md 的未驗證假設欄位非空，讀取後主動列出，詢問使用者本次是否要處理
4. 確認本 session 以下項目，確認後才開始執行
    - 你理解的本次目標
    - 完成條件
    - 主要未知點
5. 執行任務中
    - 若遇到同一問題修超過兩輪未解決——停下來說明可能的根本原因，確認方向後再繼續
    - 發現 Deviation Alerts 提到的偏移則立即提醒
6. 任務到達自然收束點時主動建議 wrap up，使用者確認後才產出輸出
    - 若本次有假設被驗證或推翻，更新 project.md 未驗證假設欄位
    - 若本次有架構改變，更新 project.md Active 區塊並將舊內容移入 Archive
    - 若本次新增了值得長期保留的 test case，於 handoff.md Last Completed 註記檔案位置
    - 主動掃描本次對話，若發現 constraints 規則需要調整，產出更新的建議
    - 每個 session 結束必須產出完整的程式碼區塊 handoff 與 commit 指令
    - commit message 由 AI 根據本次 session log 產出，格式為 git commit -m "..."

## Priority Rules

1. 問題理解正確
2. 使用成熟方案
3. 保持簡單
4. 完成功能
5. 可維護性
6. 效能

## Project Maturity

專案成熟度由「未驗證假設」與「成功定義達成程度」推導。

當未驗證假設存在時：
- 優先驗證假設
- 優先低成本實驗
- 避免過度設計

當成功定義已達成且未驗證假設已清空時：
- 可開始重構
- 可開始補測試
- 可開始優化可維護性

## Deviation Alerts

遇到以下三類情況立即暫停，說明觀察到的偏移，問使用者要修正方向、調整藍圖、還是這本來就是計畫的一部分：
- **目標偏移**：當前子任務不再直接服務主線目標
- **成本偏移**：某個工具或格式問題佔據比主線任務更多的討論篇幅
- **證明偏移**：開始追求「讓某技術細節能運作」而非完成功能
- **複雜度偏移**：為了解決簡單問題，引入額外框架、抽象層、服務、資料流，且其成本高於問題本身。

## Development Environment

- Windows 10
- PowerShell 5.1（工作）/ PowerShell 7（自用）
- Python 3.10（工作）/ Python 3.11（自用）
- Node.js 24（自用）

限制：
- 工作場所不額外安裝軟體，但可安裝 Python 套件

## Development Workflow

- 開發與測試以本機為主
- AI 有容器時可先行驗證
- Context 同步使用 GitHub Contents API
- 檔案讀寫一律使用 UTF-8

## Coding Principles

### [核心規則] 通用（所有專案）
- 優先可讀性
- 命名需表達意圖
- 註解說明 Why，具體規則：
  - 只在設計決策不明顯、有更直覺但錯誤的替代做法存在時寫
  - 只在有不能改動的限制且原因從程式碼看不出來時寫
  - 不寫描述行為的註解（程式碼本身應該能說明行為）
  - 新建檔案時在頂端加 MODULE CONTEXT，記錄職責邊界、不能隨意修改的地方及原因
- 不吞掉錯誤
- 邊界條件先處理
- 測試覆蓋判斷：核心邏輯、容易改壞且不易察覺的部分，完成後補上對應 test case；一次性腳本、UI 外觀、實驗性功能不強制
- 若該模組已有 test，修改前先執行確認現狀無誤，修改後再次執行才算完成

### [參考規則] 維護型專案適用
<!-- 非維護型專案忽略此區塊 -->
- 優先最小變更
- 保持既有介面相容
- 保持單向依賴
